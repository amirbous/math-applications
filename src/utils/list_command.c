#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>


struct Linked_command * parseLine(char *line) {

    struct Linked_command *first_word;

    int i = 0, construct_index = 0;
    char *tmp = malloc(1024);

   /* bool in_word = true;
    while (line[i] != '\n') {
        if (in_word) {
            

        }
        i++;  
    }*/
    return NULL;

}
